# Core
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
pydantic>=2.10.0

# Database
sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20.0
asyncpg>=0.30.0

# HTTP client
httpx>=0.27.2

# Streaming JSON parser (lisskins bulk)
ijson>=3.2.3

# Telegram
python-telegram-bot>=21.6
python-multipart>=0.0.12

# Env
python-dotenv>=1.0.1

# Optional: Playwright fallback для lis-skins.com
# Раскомментируй если lisskins вернёт защиту Cloudflare и httpx начнёт получать 403/503.
# Тяжёлая зависимость (~700MB браузера), для прода обычно не нужна.
# playwright>=1.40.0
